{
  "name": "batch-request-client",
  "version": "1.0.0",
  "description": "Batch requests using Axios Client",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.2"
  }
}
